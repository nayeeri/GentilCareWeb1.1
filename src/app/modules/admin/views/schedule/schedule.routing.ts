import { Route } from "@angular/router";
import { ScheduleComponent } from './schedule.component';

export const scheduleRoutingModule: Route[] = [
    {
        path : '',
        component : ScheduleComponent
    }
];
