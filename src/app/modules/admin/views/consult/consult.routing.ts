import { Route } from '@angular/router';
import { ConsultComponent } from './consult.component';

export const consultRoutingModule: Route[] = [
    {
        path : '',
        component : ConsultComponent
    }
];
