import { Route } from '@angular/router';
import { RolFormComponent } from './rol-form.component';

export const formRolRoutes: Route[] = [
    {
        path     : '',
        component: RolFormComponent
    },
];
