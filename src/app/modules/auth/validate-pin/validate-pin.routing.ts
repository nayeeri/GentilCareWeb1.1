import { Route } from '@angular/router';
import { AuthValidatePinComponent } from './validate-pin.component';


export const authValidatePinRoutes: Route[] = [
    {
        path     : '',
        component: AuthValidatePinComponent
    }
];
